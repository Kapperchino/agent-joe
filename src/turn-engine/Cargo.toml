[package]
name = "turn-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
clients = { path = "../clients" }
common-models = { path = "../common-models" }
tools = { path = "../tools" }
utils = { path = "../utils" }
anyhow = "1.0"
thiserror = "2"
serde_json = "1.0"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
